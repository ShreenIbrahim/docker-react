language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t shereen/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true shereen/docker-react npm run test
